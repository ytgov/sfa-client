// These are fundingRequest.requestType.description values
// I'm not sure if these should be references only from the database, or computed from table fields?
enum FundingSources {
  CANADA_STUDENT_FINANCIAL_ASSISTANCE_FULL_TIME = "Canada Student Financial Assistance (Full-Time)"
}

export default FundingSources
