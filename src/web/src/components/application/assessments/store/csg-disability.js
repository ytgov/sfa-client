import axios from "axios";
import { ADDRESS_TYPE } from "@/urls";

const state = {
  test: [{WATER: "er"}],
};
const getters = {};
const mutations = {};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
  namespaced: true,
};
